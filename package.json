{
  "name": "bytefest-competition-backend",
  "version": "1.0.0",
  "description": "BYTEFEST 2026 competition backend for Code Sprint, Bug Hunt and Checkmate",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check routes/admin.js && node --check routes/codesprint.js && node --check config/codesprintQuestions.js && node --check models/CodeSprintTeam.js && node --check utils/adminAuth.js && node --check utils/codeSprintAuth.js"
  },
  "engines": { "node": ">=20.19.0" },
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.2"
  },
  "devDependencies": { "nodemon": "^3.1.14" }
}
